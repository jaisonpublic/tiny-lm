# Speed run data config: shorter blocks + larger batch
# Uses the same tokenized data files — no re-tokenization needed!
train_path: "data/tinystories-8k-tokenized/train.bin"
val_path: "data/tinystories-8k-tokenized/val.bin"
block_size: 256
stride: 256
dtype: "uint16"
eos_token_id: 2  # same as original
batch_size: 128
num_workers: 4
pin_memory: true
drop_last: true
